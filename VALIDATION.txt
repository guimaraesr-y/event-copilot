Event Command Center — Mini-feature 03 validation

Validated in this build environment:
- Compose/YAML structural contract from mini-features 01/02
- n8n remains on its isolated logical PostgreSQL database
- Migrator uses `kysely/migration` and migration 003 is registered
- migration 003 tenant-scoped FKs, confirmation constraints and unique assignment
- event-vendor + outbox transaction boundaries
- full TypeScript structural compilation using local validation shims
- strict domain/EventEngine/VendorEngine compilation with local tsc
- EventEngine behavioral suite: 8 scenarios
- VendorEngine behavioral suite: 7 scenarios
- shell syntax for smoke and PostgreSQL init scripts
- smoke coverage for previous event planning plus vendor attach/request/confirm lifecycle

Not executed here:
- docker compose up / actual container startup, because Docker is not available in this execution environment.

Run locally:
  cp .env.example .env
  docker compose up --build -d
  ./scripts/smoke.sh
