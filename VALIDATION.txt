Event Command Center — Mini-feature 05 validation

Executed in the build environment:
- compose/YAML structural validation
- migrations 001..005 registration validation
- Kysely Migrator import from kysely/migration
- outbound_messages constraints/idempotency contract validation
- Domain Event Gateway JSON structure validation
- WhatsApp Status Gateway JSON structure validation
- n8n import/publish/restart script validation
- Caddy internal boundary validation
- full structural TypeScript compilation
- strict core TypeScript compilation
- shell syntax validation
- EventEngine behavioral tests: 8/8
- VendorEngine behavioral tests: 7/7
- MessagingEngine behavioral tests: 6/6
- ZIP integrity validation

Not executable in this build environment:
- docker compose up / real n8n runtime execution (Docker is unavailable here)
- Meta WhatsApp Cloud send (requires user credentials and a configured Graph API version)

The included scripts/smoke.sh is the authoritative local integration test. It uses WHATSAPP_PROVIDER=mock and validates outbox -> n8n -> automation action -> outbound message -> mock send -> delivery/read callbacks -> global outbox acknowledgement.
